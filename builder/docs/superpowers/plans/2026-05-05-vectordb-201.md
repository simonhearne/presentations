# Vector Databases 201 Deck Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Author a `talks/vectordb-201/` deck — a follow-on to `talks/vectordb-101/` covering production vector-DB systems (index algorithms, hybrid search, distribution, observability).

**Architecture:** Pure content. No build-pipeline changes. Authored as one `slides.md` plus three Vega JSON specs, using existing `vega` and `dot` frontmatter. Each task delivers a coherent slice of the deck, ends with a successful `npm run build talks/vectordb-201`, and commits.

**Tech Stack:** Existing pipeline — `marked`, `@hpcc-js/wasm-graphviz`, Vega/Vega-Lite, brand CSS in [style/](../../../style/).

**Spec:** [docs/superpowers/specs/2026-05-05-vectordb-201-design.md](../specs/2026-05-05-vectordb-201-design.md)

**Voice anchor:** The 101 deck at [talks/vectordb-101/slides.md](../../../talks/vectordb-101/slides.md) is the canonical voice reference. Match its tone: short declarative sentences, concrete numbers, decision tables, no hedging, occasional one-liner punchlines. Slides typically have 3–6 lines of prose plus one structural element (table, code, chart, diagram). When in doubt, cut a sentence.

**File structure:**
- Create: `talks/vectordb-201/slides.md` (grown across tasks)
- Create: `talks/vectordb-201/ivf-voronoi.json`
- Create: `talks/vectordb-201/recall-latency.json`
- Create: `talks/vectordb-201/hybrid-fusion.json`
- The `dist/` subdirectory is build output and gitignored.

**Order rationale:** Build top-to-bottom in slide order so visual review is incremental and natural. Each task's verification step builds the deck and confirms the new slides appear; reviewer can open `dist/index.html` between tasks. Vega specs are built in the same task as the slide that references them so each commit is self-contained and the build never points at a missing file.

**Per-task verification protocol** (apply to every task unless noted otherwise):

1. Run `npm run build talks/vectordb-201` — must exit 0.
2. Run `grep -c '<section' talks/vectordb-201/dist/index.html` — count must equal the cumulative slide count after this task. (Each task lists the expected count.)
3. For tasks adding a Vega spec: confirm the spec file is referenced in `slides.md` via a `vega` block, and that `node -e "JSON.parse(require('fs').readFileSync('talks/vectordb-201/<spec>.json'))"` succeeds.
4. For tasks adding a dot block: confirm `grep -c 'class="dot"' talks/vectordb-201/dist/index.html` increased by the expected number.
5. Run `npm test` — must pass (no code changed but this catches accidental damage).
6. Commit with conventional-commit prefix `feat(vectordb-201):`.

---

## Task 1: Scaffold deck directory and write title + intro slides

**Files:**
- Create: `talks/vectordb-201/slides.md`

**Cumulative slide count after task: 3**

- [ ] **Step 1: Create the directory and write `slides.md` with the three intro slides**

Create `talks/vectordb-201/slides.md` containing exactly:

````markdown
{.title .no-chrome}
<img class="logo" src="../../../img/zilliz-light.svg" alt="">

# Vector Databases 201
## Production-grade systems

```authors
- name: Simon Hearne
  position: Solutions Architect
  company: Zilliz
  photo: https://avatars.githubusercontent.com/u/496189?v=4
```

---

# You're here because 101 clicked

The first deck got you to *meaning is a coordinate*. That's the toy.

Production is the rest of the iceberg:

- Picking an index that survives your QPS and your RAM budget
- Hybrid search when dense alone misses the obvious
- Sharding, replication, and keeping tenants out of each other's hair
- Knowing when something is silently breaking — before the business does

---

# What you'll leave with

Four decision tables and the mental model behind each:

1. **Index choice** — HNSW vs IVF vs DiskANN, on five axes
2. **Hybrid pipeline** — when each stage is worth its latency
3. **Topology** — sharding, replicas, multi-tenancy patterns
4. **Alerts** — the three you actually need

Concept-first, with `pymilvus` for the concrete shape.
````

- [ ] **Step 2: Verify the deck builds**

Run: `npm run build talks/vectordb-201`

Expected: exits 0, prints a "Built" line, creates `talks/vectordb-201/dist/index.html`.

- [ ] **Step 3: Verify slide count**

Run: `grep -c '<section' talks/vectordb-201/dist/index.html`

Expected: `3`

- [ ] **Step 4: Run tests**

Run: `npm test`

Expected: all tests pass.

- [ ] **Step 5: Commit**

```bash
git add talks/vectordb-201/slides.md
git commit -m "feat(vectordb-201): scaffold deck with title and intro slides"
```

---

## Task 2: Section 1 opener — ANN tradeoff space and HNSW

**Files:**
- Modify: `talks/vectordb-201/slides.md` (append section divider + 4 slides)

**Cumulative slide count after task: 8** (3 + section divider + 4 content)

- [ ] **Step 1: Append the section divider and the four HNSW slides**

Append to `talks/vectordb-201/slides.md`:

````markdown
---

{.section}
# Indexes that don't fall over
## 01

---

# The ANN tradeoff space

No index wins on every axis. You're picking which to lose:

| Axis | What it costs you |
|---|---|
| **Recall** | how often the true top-k makes it back |
| **Query latency** | p99 in ms |
| **Memory** | bytes per vector resident in RAM |
| **Build time** | seconds to absorb a million new vectors |
| **Insert cost** | latency hit for live writes vs batch rebuilds |

Three families dominate: HNSW, IVF, and DiskANN. They lose on different axes.

---

# HNSW — graphs all the way down

A multi-layer proximity graph. Sparse at the top, dense at the bottom. Search starts at a top-layer entry point, greedily descends, and converges in `O(log n)`-ish hops.

```dot
rankdir=TB
L2a [label="L2"]
L2b [label="L2"]
L1a [label="L1"]
L1b [label="L1"]
L1c [label="L1"]
L1d [label="L1"]
L0a [label="L0"]
L0b [label="L0"]
L0c [label="L0"]
L0d [label="L0"]
L0e [label="L0"]
L0f [label="L0"]
L2a -> L2b
L2a -> L1a
L2b -> L1c
L1a -> L1b -> L1c -> L1d
L1a -> L0a
L1b -> L0b
L1c -> L0d
L1d -> L0e
L0a -> L0b -> L0c -> L0d -> L0e -> L0f
```

The graph itself is the index. The index lives in RAM.

---

# HNSW knobs

| Param | What it does | Bigger means |
|---|---|---|
| **`M`** | edges per node | better recall, more RAM, slower build |
| **`efConstruction`** | candidate-list width during build | better graph quality, slower build |
| **`efSearch`** | candidate-list width per query | better recall, slower query |

Build-time knobs (`M`, `efConstruction`) are baked in. Query-time knob (`efSearch`) is a dial you can twist per request.

Reasonable starting point: `M=16`, `efConstruction=200`, `efSearch=64`. Move from there.

---

# HNSW sweet spot

Reach for HNSW when:

- Recall must be ≥ 0.95
- p99 latency budget is tight (sub-20ms)
- Working set fits in RAM (figure 2–3× raw vector bytes)
- Inserts are batched, not write-heavy
- QPS is the dominant cost

This is the 101's "default recommendation" index. It's the default for a reason.
````

- [ ] **Step 2: Build and verify**

Run: `npm run build talks/vectordb-201 && grep -c '<section' talks/vectordb-201/dist/index.html`

Expected: build succeeds, count is `8`.

- [ ] **Step 3: Verify the dot diagram rendered**

Run: `grep -c 'class="dot"' talks/vectordb-201/dist/index.html`

Expected: `1`.

- [ ] **Step 4: Run tests**

Run: `npm test`

Expected: pass.

- [ ] **Step 5: Commit**

```bash
git add talks/vectordb-201/slides.md
git commit -m "feat(vectordb-201): add Section 1 opener and HNSW slides"
```

---

## Task 3: Build IVF Voronoi vega spec and IVF slides

**Files:**
- Create: `talks/vectordb-201/ivf-voronoi.json`
- Modify: `talks/vectordb-201/slides.md` (append 3 slides)

**Cumulative slide count after task: 11**

- [ ] **Step 1: Create `ivf-voronoi.json`**

Create `talks/vectordb-201/ivf-voronoi.json`:

```json
{
  "$schema": "https://vega.github.io/schema/vega/v5.json",
  "description": "IVF intuition — pick a query point, see which nprobe cells it searches.",
  "width": 900,
  "height": 520,
  "padding": 24,
  "background": "transparent",
  "config": {
    "axis": { "labelFontSize": 12, "titleFontSize": 14, "domainColor": "#bbb", "tickColor": "#bbb" },
    "view": { "stroke": "transparent" }
  },
  "signals": [
    { "name": "nprobe", "value": 3, "bind": { "input": "range", "min": 1, "max": 16, "step": 1, "name": "nprobe " } },
    { "name": "qx", "value": 5, "on": [ { "events": "click", "update": "invert('xscale', x())" } ] },
    { "name": "qy", "value": 5, "on": [ { "events": "click", "update": "invert('yscale', y())" } ] }
  ],
  "data": [
    {
      "name": "centroids",
      "values": [
        {"id": 0, "x": 1.5, "y": 1.5}, {"id": 1, "x": 4.0, "y": 1.0}, {"id": 2, "x": 7.0, "y": 2.0}, {"id": 3, "x": 9.0, "y": 1.5},
        {"id": 4, "x": 1.0, "y": 4.0}, {"id": 5, "x": 3.5, "y": 4.5}, {"id": 6, "x": 6.0, "y": 4.0}, {"id": 7, "x": 8.5, "y": 4.5},
        {"id": 8, "x": 1.5, "y": 6.5}, {"id": 9, "x": 4.0, "y": 7.0}, {"id": 10, "x": 6.5, "y": 6.5}, {"id": 11, "x": 9.0, "y": 7.0},
        {"id": 12, "x": 2.0, "y": 9.0}, {"id": 13, "x": 4.5, "y": 9.0}, {"id": 14, "x": 7.0, "y": 9.0}, {"id": 15, "x": 9.5, "y": 8.5}
      ]
    },
    {
      "name": "centroids_with_dist",
      "source": "centroids",
      "transform": [
        { "type": "formula", "as": "dist", "expr": "sqrt(pow(datum.x - qx, 2) + pow(datum.y - qy, 2))" },
        { "type": "window", "ops": ["row_number"], "as": ["rank"], "sort": { "field": "dist" } },
        { "type": "formula", "as": "probed", "expr": "datum.rank <= nprobe" }
      ]
    },
    {
      "name": "voronoi",
      "source": "centroids_with_dist",
      "transform": [
        { "type": "voronoi", "x": "x", "y": "y", "size": [ {"signal": "width"}, {"signal": "height"} ] }
      ]
    },
    {
      "name": "query",
      "values": [ {"x": 5, "y": 5} ],
      "transform": [
        { "type": "formula", "as": "x", "expr": "qx" },
        { "type": "formula", "as": "y", "expr": "qy" }
      ]
    }
  ],
  "scales": [
    { "name": "xscale", "type": "linear", "domain": [0, 10], "range": "width" },
    { "name": "yscale", "type": "linear", "domain": [0, 10], "range": "height" }
  ],
  "marks": [
    {
      "type": "path",
      "from": { "data": "voronoi" },
      "encode": {
        "enter": { "stroke": { "value": "#bbb" }, "strokeWidth": { "value": 1 } },
        "update": {
          "path": { "field": "path" },
          "fill": [
            { "test": "datum.probed", "value": "#1f6feb" },
            { "value": "#f6f8fa" }
          ],
          "fillOpacity": [
            { "test": "datum.probed", "value": 0.25 },
            { "value": 1 }
          ]
        }
      }
    },
    {
      "type": "symbol",
      "from": { "data": "centroids_with_dist" },
      "encode": {
        "enter": { "size": { "value": 60 }, "shape": { "value": "circle" } },
        "update": {
          "x": { "scale": "xscale", "field": "x" },
          "y": { "scale": "yscale", "field": "y" },
          "fill": [
            { "test": "datum.probed", "value": "#0a2540" },
            { "value": "#9aa4b2" }
          ]
        }
      }
    },
    {
      "type": "symbol",
      "from": { "data": "query" },
      "encode": {
        "enter": { "size": { "value": 220 }, "shape": { "value": "diamond" }, "fill": { "value": "#c84cff" }, "stroke": { "value": "#fff" }, "strokeWidth": { "value": 2 } },
        "update": {
          "x": { "scale": "xscale", "field": "x" },
          "y": { "scale": "yscale", "field": "y" }
        }
      }
    }
  ]
}
```

- [ ] **Step 2: Verify the JSON parses**

Run: `node -e "JSON.parse(require('fs').readFileSync('talks/vectordb-201/ivf-voronoi.json','utf8')); console.log('ok')"`

Expected: prints `ok`.

- [ ] **Step 3: Append the three IVF slides to `slides.md`**

Append:

````markdown
---

# IVF — partition first, search second

Cluster all vectors into `nlist` Voronoi cells (k-means, offline). At query time, find the `nprobe` cells closest to the query and search only those.

```vega
- spec: ./ivf-voronoi.json
  renderer: svg
  actions: false
```

Click anywhere to drop a query. Slide `nprobe` to widen the search. More cells probed → higher recall, higher latency.

---

# IVF knobs

| Param | What it does | Bigger means |
|---|---|---|
| **`nlist`** | number of cells (set at build) | finer partitions, slower build, more centroids in RAM |
| **`nprobe`** | cells searched per query | higher recall, slower query |
| **PQ codes** | optional per-vector compression | smaller index, lower recall |

Rule of thumb: `nlist ≈ √n`. So a 10M-vector collection wants ~3000 cells.

`IVF_FLAT` keeps full vectors. `IVF_PQ` quantizes them. `IVF_SQ` is the cheap middle ground.

---

# IVF sweet spot

Reach for IVF when:

- Dataset is bigger than RAM (`IVF_PQ` lets you fit 100M vectors in 10s of GB)
- Ingest is batchy — full rebuilds are tolerable
- Recall budget is relaxed (0.85–0.95)
- QPS is moderate

The classic "we have lots of vectors and not lots of RAM" choice.
````

- [ ] **Step 4: Build and verify**

Run: `npm run build talks/vectordb-201`

Expected: exits 0. Vega script tags should appear in output (only when at least one chart exists).

Run: `grep -c '<section' talks/vectordb-201/dist/index.html`

Expected: `11`.

- [ ] **Step 5: Run tests**

Run: `npm test`

Expected: pass.

- [ ] **Step 6: Commit**

```bash
git add talks/vectordb-201/ivf-voronoi.json talks/vectordb-201/slides.md
git commit -m "feat(vectordb-201): add IVF slides with interactive Voronoi spec"
```

---

## Task 4: DiskANN slides

**Files:**
- Modify: `talks/vectordb-201/slides.md` (append 2 slides)

**Cumulative slide count after task: 13**

- [ ] **Step 1: Append the two DiskANN slides**

Append:

````markdown
---

# DiskANN — index on SSD, recall in RAM

A Vamana graph that lives on disk. PQ-compressed vectors stay in RAM as a small "summary" index. Query traverses the graph on disk; the in-RAM PQ codes do the heavy filtering before the I/O.

```dot
rankdir=LR
subgraph cluster_ram {
  label="RAM"
  style=dashed
  color="#1f6feb"
  P [label="PQ codes\n(per-vector summary)"]
}
subgraph cluster_ssd {
  label="SSD"
  style=dashed
  color="#9aa4b2"
  V [label="Vamana graph\n+ full vectors" shape=cylinder]
}
Q [label="Query"]
R [label="Top-k"]
Q -> P
P -> V [label="candidate IDs"]
V -> R
```

The trick: SSD is 100× cheaper per byte than RAM. DiskANN uses that without giving up the latency you'd lose to a naive "store on disk" index.

---

# DiskANN economics

The cost story at scale:

- 100M × 768d × 4 bytes = **307 GB** raw vectors
- HNSW in RAM: ~600 GB resident (graph overhead). On AWS r6i, that's ~$4/hr per instance, plus replicas.
- DiskANN: ~15–30 GB RAM (PQ codes) + 300 GB SSD. Comparable QPS, fraction of the cost.

p99 takes a hit — typically 30–80 ms vs HNSW's 5–20 ms. If your latency budget allows, the cost delta is hard to argue with.
````

- [ ] **Step 2: Build and verify**

Run: `npm run build talks/vectordb-201 && grep -c '<section' talks/vectordb-201/dist/index.html`

Expected: build succeeds, count is `13`.

- [ ] **Step 3: Verify dot count increased**

Run: `grep -c 'class="dot"' talks/vectordb-201/dist/index.html`

Expected: `2`.

- [ ] **Step 4: Run tests, commit**

Run: `npm test`

Expected: pass.

```bash
git add talks/vectordb-201/slides.md
git commit -m "feat(vectordb-201): add DiskANN slides"
```

---

## Task 5: Build recall-vs-latency vega spec and the interactive slide

**Files:**
- Create: `talks/vectordb-201/recall-latency.json`
- Modify: `talks/vectordb-201/slides.md` (append 1 slide)

**Cumulative slide count after task: 14**

The numbers in this spec are illustrative shapes, not benchmarks. The slide explicitly labels them as such.

- [ ] **Step 1: Create `recall-latency.json`**

Create `talks/vectordb-201/recall-latency.json`:

```json
{
  "$schema": "https://vega.github.io/schema/vega-lite/v5.json",
  "description": "Recall vs latency — illustrative shapes for HNSW, IVF, DiskANN.",
  "width": 760,
  "height": 460,
  "padding": 24,
  "background": "transparent",
  "config": {
    "axis": { "labelFontSize": 12, "titleFontSize": 14, "domainColor": "#bbb", "tickColor": "#bbb" },
    "view": { "stroke": "transparent" },
    "legend": { "labelFontSize": 13, "titleFontSize": 13 }
  },
  "params": [
    {
      "name": "scale",
      "value": "10M",
      "bind": { "input": "select", "name": "Dataset size ", "options": ["1M", "10M", "100M"] }
    }
  ],
  "data": {
    "values": [
      {"index": "HNSW",    "scale": "1M",   "param": 16,  "recall": 0.82, "latency": 0.9},
      {"index": "HNSW",    "scale": "1M",   "param": 32,  "recall": 0.91, "latency": 1.6},
      {"index": "HNSW",    "scale": "1M",   "param": 64,  "recall": 0.96, "latency": 2.8},
      {"index": "HNSW",    "scale": "1M",   "param": 128, "recall": 0.985, "latency": 5.4},
      {"index": "HNSW",    "scale": "1M",   "param": 256, "recall": 0.995, "latency": 11.0},
      {"index": "HNSW",    "scale": "10M",  "param": 16,  "recall": 0.78, "latency": 3.2},
      {"index": "HNSW",    "scale": "10M",  "param": 32,  "recall": 0.88, "latency": 5.8},
      {"index": "HNSW",    "scale": "10M",  "param": 64,  "recall": 0.95, "latency": 9.6},
      {"index": "HNSW",    "scale": "10M",  "param": 128, "recall": 0.98, "latency": 18.0},
      {"index": "HNSW",    "scale": "10M",  "param": 256, "recall": 0.992, "latency": 36.0},
      {"index": "HNSW",    "scale": "100M", "param": 16,  "recall": 0.72, "latency": 14.0},
      {"index": "HNSW",    "scale": "100M", "param": 32,  "recall": 0.84, "latency": 24.0},
      {"index": "HNSW",    "scale": "100M", "param": 64,  "recall": 0.93, "latency": 42.0},
      {"index": "HNSW",    "scale": "100M", "param": 128, "recall": 0.97, "latency": 78.0},
      {"index": "HNSW",    "scale": "100M", "param": 256, "recall": 0.985, "latency": 150.0},
      {"index": "IVF",     "scale": "1M",   "param": 4,   "recall": 0.62, "latency": 0.7},
      {"index": "IVF",     "scale": "1M",   "param": 16,  "recall": 0.84, "latency": 1.4},
      {"index": "IVF",     "scale": "1M",   "param": 64,  "recall": 0.94, "latency": 3.5},
      {"index": "IVF",     "scale": "1M",   "param": 256, "recall": 0.985, "latency": 9.5},
      {"index": "IVF",     "scale": "10M",  "param": 4,   "recall": 0.55, "latency": 2.3},
      {"index": "IVF",     "scale": "10M",  "param": 16,  "recall": 0.78, "latency": 5.2},
      {"index": "IVF",     "scale": "10M",  "param": 64,  "recall": 0.91, "latency": 12.0},
      {"index": "IVF",     "scale": "10M",  "param": 256, "recall": 0.975, "latency": 32.0},
      {"index": "IVF",     "scale": "100M", "param": 4,   "recall": 0.48, "latency": 9.0},
      {"index": "IVF",     "scale": "100M", "param": 16,  "recall": 0.72, "latency": 22.0},
      {"index": "IVF",     "scale": "100M", "param": 64,  "recall": 0.88, "latency": 55.0},
      {"index": "IVF",     "scale": "100M", "param": 256, "recall": 0.965, "latency": 140.0},
      {"index": "DiskANN", "scale": "1M",   "param": 20,  "recall": 0.82, "latency": 4.0},
      {"index": "DiskANN", "scale": "1M",   "param": 50,  "recall": 0.92, "latency": 8.5},
      {"index": "DiskANN", "scale": "1M",   "param": 100, "recall": 0.97, "latency": 15.0},
      {"index": "DiskANN", "scale": "1M",   "param": 200, "recall": 0.99, "latency": 26.0},
      {"index": "DiskANN", "scale": "10M",  "param": 20,  "recall": 0.80, "latency": 14.0},
      {"index": "DiskANN", "scale": "10M",  "param": 50,  "recall": 0.91, "latency": 26.0},
      {"index": "DiskANN", "scale": "10M",  "param": 100, "recall": 0.965, "latency": 42.0},
      {"index": "DiskANN", "scale": "10M",  "param": 200, "recall": 0.987, "latency": 70.0},
      {"index": "DiskANN", "scale": "100M", "param": 20,  "recall": 0.78, "latency": 36.0},
      {"index": "DiskANN", "scale": "100M", "param": 50,  "recall": 0.90, "latency": 62.0},
      {"index": "DiskANN", "scale": "100M", "param": 100, "recall": 0.96, "latency": 95.0},
      {"index": "DiskANN", "scale": "100M", "param": 200, "recall": 0.985, "latency": 160.0}
    ]
  },
  "transform": [ { "filter": "datum.scale == scale" } ],
  "mark": { "type": "line", "point": { "size": 80, "filled": true } },
  "encoding": {
    "x": { "field": "latency", "type": "quantitative", "title": "p99 latency (ms, log)", "scale": { "type": "log" } },
    "y": { "field": "recall", "type": "quantitative", "title": "recall@10", "scale": { "domain": [0.4, 1.0] } },
    "color": {
      "field": "index",
      "type": "nominal",
      "scale": { "domain": ["HNSW", "IVF", "DiskANN"], "range": ["#1f6feb", "#c84cff", "#0a2540"] },
      "title": "Index"
    }
  }
}
```

- [ ] **Step 2: Verify the JSON parses**

Run: `node -e "JSON.parse(require('fs').readFileSync('talks/vectordb-201/recall-latency.json','utf8')); console.log('ok')"`

Expected: prints `ok`.

- [ ] **Step 3: Append the slide**

Append:

````markdown
---

# Recall vs latency — pick a Pareto point

Sweep each index's main quality knob. Same data, three curves. Switch dataset size to see how each scales.

```vega
- spec: ./recall-latency.json
  renderer: svg
  actions: false
```

Numbers are illustrative shapes, not benchmarks. The point is the *shape* of each curve — HNSW's wall on the left, DiskANN's flatter slope at the top, IVF's tunability.
````

- [ ] **Step 4: Build, verify, test, commit**

Run: `npm run build talks/vectordb-201 && grep -c '<section' talks/vectordb-201/dist/index.html`

Expected: build succeeds, count is `14`.

Run: `npm test`

Expected: pass.

```bash
git add talks/vectordb-201/recall-latency.json talks/vectordb-201/slides.md
git commit -m "feat(vectordb-201): add interactive recall-vs-latency chart"
```

---

## Task 6: Section 1 closer — decision table and Milvus index configs

**Files:**
- Modify: `talks/vectordb-201/slides.md` (append 2 slides)

**Cumulative slide count after task: 16**

- [ ] **Step 1: Append the two closer slides**

Append:

````markdown
---

# Picking an index

| Index | Recall ceiling | Latency floor | Memory | Best for |
|---|---|---|---|---|
| **HNSW** | 0.99+ | 1–10 ms | 2–3× raw | high QPS, RAM-rich, batch ingest |
| **IVF_PQ** | 0.95 | 5–30 ms | 0.1–0.3× raw | huge dataset, tight RAM, batch ingest |
| **DiskANN** | 0.98 | 30–80 ms | 0.1× raw + SSD | billion-scale, cost-sensitive |

If you can fit the working set in RAM and need sub-10ms — HNSW. If not — DiskANN before IVF_PQ unless you're already operating IVF.

---

# Same data, three index configs

```python
from pymilvus import MilvusClient

client = MilvusClient(uri="./milvus.db")

def make_params(index_type, params):
    p = client.prepare_index_params()
    p.add_index(field_name="vector", index_type=index_type,
                metric_type="COSINE", params=params)
    return p

# HNSW — RAM-resident, high recall, low latency
client.create_index(collection_name="docs", index_params=make_params(
    "HNSW", {"M": 16, "efConstruction": 200}))

# IVF_PQ — compressed, lower memory
client.create_index(collection_name="docs", index_params=make_params(
    "IVF_PQ", {"nlist": 1024, "m": 16, "nbits": 8}))

# DiskANN — graph on SSD
client.create_index(collection_name="docs", index_params=make_params(
    "DISKANN", {"search_list": 100}))
```

Three lines change. The rest is the same collection, the same data, the same queries.
````

- [ ] **Step 2: Build, verify, test, commit**

Run: `npm run build talks/vectordb-201 && grep -c '<section' talks/vectordb-201/dist/index.html`

Expected: build succeeds, count is `16`.

Run: `npm test`

Expected: pass.

```bash
git add talks/vectordb-201/slides.md
git commit -m "feat(vectordb-201): close Section 1 with decision table and Milvus configs"
```

---

## Task 7: Section 2 opener — hybrid search motivation through score fusion

**Files:**
- Modify: `talks/vectordb-201/slides.md` (append section divider + 4 slides)

**Cumulative slide count after task: 21** (16 + 1 divider + 4 content)

- [ ] **Step 1: Append the section divider and four slides**

Append:

````markdown
---

{.section}
# Hybrid search & reranking
## 02

---

# Where dense alone fails

Dense embeddings encode *meaning*. They are remarkably bad at:

- **Rare terms** — `K8s 1.29 CSI driver`, `RFC 9421`
- **IDs and codes** — `INV-204871`, `pod-7f4b`
- **Exact phrases** — `"the quick brown fox"` as a literal quote
- **Recency** — yesterday's outage doesn't have a meaning yet

Try semantic search on `ERROR: ECONNRESET in pod-7f4b`. It returns generic "connection error" docs and ignores the pod name — the most diagnostic token in the query.

---

# BM25 in 60 seconds

Score a document for a query by combining:

- **Term frequency** — does this doc contain the query terms, and how often
- **Inverse document frequency** — rare terms are worth more
- **Length normalization** — long docs aren't penalized for being long

Decades-old, well-understood, blindingly fast. Sparse vectors: each dimension is a term, most values are zero.

BM25 is a vector search too. It just lives in a different geometry.

---

# Two paths, one result list

```dot
rankdir=LR
Q [label="Query"]
B [label="BM25\nretriever"]
D [label="Dense\nretriever"]
F [label="Score fusion\n(RRF)"]
R [label="Reranker\n(cross-encoder)"]
T [label="Top-k"]
Q -> B
Q -> D
B -> F
D -> F
F -> R -> T
```

Run both. Fuse. Rerank. The whole pipeline is ~50–250 ms depending on the reranker.

---

# Score fusion, three ways

| Method | Formula | When |
|---|---|---|
| **RRF** | `1/(k+rank)` summed across retrievers | the default — no normalization needed |
| **Weighted sum** | `α·s_dense + (1-α)·s_bm25` | when scores are pre-calibrated |
| **Convex combination** | per-query learned `α` | research-grade, rarely worth it |

RRF wins because it sidesteps score normalization entirely — different retrievers produce scores on different scales, and trying to reconcile them is a swamp. Use ranks, not scores.

`k=60` is the canonical RRF constant. Don't tune it.
````

- [ ] **Step 2: Build, verify dot count, test, commit**

Run: `npm run build talks/vectordb-201 && grep -c '<section' talks/vectordb-201/dist/index.html`

Expected: build succeeds, count is `21`.

Run: `grep -c 'class="dot"' talks/vectordb-201/dist/index.html`

Expected: `3`.

Run: `npm test`

Expected: pass.

```bash
git add talks/vectordb-201/slides.md
git commit -m "feat(vectordb-201): open Section 2 with hybrid search motivation and fusion"
```

---

## Task 8: Build hybrid-fusion vega spec and the interactive slide

**Files:**
- Create: `talks/vectordb-201/hybrid-fusion.json`
- Modify: `talks/vectordb-201/slides.md` (append 1 slide)

**Cumulative slide count after task: 22**

- [ ] **Step 1: Create `hybrid-fusion.json`**

Create `talks/vectordb-201/hybrid-fusion.json`:

```json
{
  "$schema": "https://vega.github.io/schema/vega-lite/v5.json",
  "description": "One query, four columns — BM25, dense, fused, reranked.",
  "width": 760,
  "height": 360,
  "padding": 24,
  "background": "transparent",
  "config": {
    "axis": { "labelFontSize": 12, "titleFontSize": 13, "domainColor": "#bbb", "tickColor": "#bbb" },
    "view": { "stroke": "transparent" }
  },
  "params": [
    {
      "name": "query",
      "value": "ECONNRESET in pod-7f4b",
      "bind": { "input": "select", "name": "Query ", "options": [
        "ECONNRESET in pod-7f4b",
        "how does HNSW work",
        "INV-204871 refund status"
      ] }
    }
  ],
  "data": {
    "values": [
      {"query":"ECONNRESET in pod-7f4b","doc":"runbook: pod-7f4b restart loop","stage":"BM25",     "rank":1},
      {"query":"ECONNRESET in pod-7f4b","doc":"k8s networking primer",         "stage":"BM25",     "rank":4},
      {"query":"ECONNRESET in pod-7f4b","doc":"TCP error reference",           "stage":"BM25",     "rank":2},
      {"query":"ECONNRESET in pod-7f4b","doc":"AWS NLB idle timeout",          "stage":"BM25",     "rank":5},
      {"query":"ECONNRESET in pod-7f4b","doc":"connection error patterns",     "stage":"BM25",     "rank":3},
      {"query":"ECONNRESET in pod-7f4b","doc":"runbook: pod-7f4b restart loop","stage":"Dense",    "rank":5},
      {"query":"ECONNRESET in pod-7f4b","doc":"k8s networking primer",         "stage":"Dense",    "rank":2},
      {"query":"ECONNRESET in pod-7f4b","doc":"TCP error reference",           "stage":"Dense",    "rank":4},
      {"query":"ECONNRESET in pod-7f4b","doc":"AWS NLB idle timeout",          "stage":"Dense",    "rank":3},
      {"query":"ECONNRESET in pod-7f4b","doc":"connection error patterns",     "stage":"Dense",    "rank":1},
      {"query":"ECONNRESET in pod-7f4b","doc":"runbook: pod-7f4b restart loop","stage":"Fused",    "rank":1},
      {"query":"ECONNRESET in pod-7f4b","doc":"k8s networking primer",         "stage":"Fused",    "rank":4},
      {"query":"ECONNRESET in pod-7f4b","doc":"TCP error reference",           "stage":"Fused",    "rank":2},
      {"query":"ECONNRESET in pod-7f4b","doc":"AWS NLB idle timeout",          "stage":"Fused",    "rank":5},
      {"query":"ECONNRESET in pod-7f4b","doc":"connection error patterns",     "stage":"Fused",    "rank":3},
      {"query":"ECONNRESET in pod-7f4b","doc":"runbook: pod-7f4b restart loop","stage":"Reranked", "rank":1},
      {"query":"ECONNRESET in pod-7f4b","doc":"k8s networking primer",         "stage":"Reranked", "rank":5},
      {"query":"ECONNRESET in pod-7f4b","doc":"TCP error reference",           "stage":"Reranked", "rank":3},
      {"query":"ECONNRESET in pod-7f4b","doc":"AWS NLB idle timeout",          "stage":"Reranked", "rank":2},
      {"query":"ECONNRESET in pod-7f4b","doc":"connection error patterns",     "stage":"Reranked", "rank":4},

      {"query":"how does HNSW work","doc":"HNSW original paper",          "stage":"BM25",     "rank":1},
      {"query":"how does HNSW work","doc":"vector index overview blog",   "stage":"BM25",     "rank":4},
      {"query":"how does HNSW work","doc":"graph algorithms textbook",    "stage":"BM25",     "rank":2},
      {"query":"how does HNSW work","doc":"approximate NN survey",        "stage":"BM25",     "rank":3},
      {"query":"how does HNSW work","doc":"Milvus index docs",            "stage":"BM25",     "rank":5},
      {"query":"how does HNSW work","doc":"HNSW original paper",          "stage":"Dense",    "rank":2},
      {"query":"how does HNSW work","doc":"vector index overview blog",   "stage":"Dense",    "rank":1},
      {"query":"how does HNSW work","doc":"graph algorithms textbook",    "stage":"Dense",    "rank":5},
      {"query":"how does HNSW work","doc":"approximate NN survey",        "stage":"Dense",    "rank":3},
      {"query":"how does HNSW work","doc":"Milvus index docs",            "stage":"Dense",    "rank":4},
      {"query":"how does HNSW work","doc":"HNSW original paper",          "stage":"Fused",    "rank":1},
      {"query":"how does HNSW work","doc":"vector index overview blog",   "stage":"Fused",    "rank":2},
      {"query":"how does HNSW work","doc":"graph algorithms textbook",    "stage":"Fused",    "rank":5},
      {"query":"how does HNSW work","doc":"approximate NN survey",        "stage":"Fused",    "rank":3},
      {"query":"how does HNSW work","doc":"Milvus index docs",            "stage":"Fused",    "rank":4},
      {"query":"how does HNSW work","doc":"HNSW original paper",          "stage":"Reranked", "rank":2},
      {"query":"how does HNSW work","doc":"vector index overview blog",   "stage":"Reranked", "rank":1},
      {"query":"how does HNSW work","doc":"graph algorithms textbook",    "stage":"Reranked", "rank":5},
      {"query":"how does HNSW work","doc":"approximate NN survey",        "stage":"Reranked", "rank":4},
      {"query":"how does HNSW work","doc":"Milvus index docs",            "stage":"Reranked", "rank":3},

      {"query":"INV-204871 refund status","doc":"invoice INV-204871 record",      "stage":"BM25",     "rank":1},
      {"query":"INV-204871 refund status","doc":"refund policy doc",              "stage":"BM25",     "rank":3},
      {"query":"INV-204871 refund status","doc":"customer support FAQ",           "stage":"BM25",     "rank":5},
      {"query":"INV-204871 refund status","doc":"billing system overview",        "stage":"BM25",     "rank":4},
      {"query":"INV-204871 refund status","doc":"refund processing runbook",      "stage":"BM25",     "rank":2},
      {"query":"INV-204871 refund status","doc":"invoice INV-204871 record",      "stage":"Dense",    "rank":5},
      {"query":"INV-204871 refund status","doc":"refund policy doc",              "stage":"Dense",    "rank":1},
      {"query":"INV-204871 refund status","doc":"customer support FAQ",           "stage":"Dense",    "rank":3},
      {"query":"INV-204871 refund status","doc":"billing system overview",        "stage":"Dense",    "rank":2},
      {"query":"INV-204871 refund status","doc":"refund processing runbook",      "stage":"Dense",    "rank":4},
      {"query":"INV-204871 refund status","doc":"invoice INV-204871 record",      "stage":"Fused",    "rank":1},
      {"query":"INV-204871 refund status","doc":"refund policy doc",              "stage":"Fused",    "rank":2},
      {"query":"INV-204871 refund status","doc":"customer support FAQ",           "stage":"Fused",    "rank":5},
      {"query":"INV-204871 refund status","doc":"billing system overview",        "stage":"Fused",    "rank":4},
      {"query":"INV-204871 refund status","doc":"refund processing runbook",      "stage":"Fused",    "rank":3},
      {"query":"INV-204871 refund status","doc":"invoice INV-204871 record",      "stage":"Reranked", "rank":1},
      {"query":"INV-204871 refund status","doc":"refund policy doc",              "stage":"Reranked", "rank":4},
      {"query":"INV-204871 refund status","doc":"customer support FAQ",           "stage":"Reranked", "rank":5},
      {"query":"INV-204871 refund status","doc":"billing system overview",        "stage":"Reranked", "rank":3},
      {"query":"INV-204871 refund status","doc":"refund processing runbook",      "stage":"Reranked", "rank":2}
    ]
  },
  "transform": [ { "filter": "datum.query == query" } ],
  "mark": "rect",
  "encoding": {
    "x": {
      "field": "stage",
      "type": "nominal",
      "sort": ["BM25", "Dense", "Fused", "Reranked"],
      "title": null,
      "axis": { "orient": "top", "labelFontSize": 14 }
    },
    "y": {
      "field": "doc",
      "type": "nominal",
      "title": null,
      "axis": { "labelFontSize": 12 }
    },
    "color": {
      "field": "rank",
      "type": "quantitative",
      "scale": { "scheme": "blues", "reverse": true, "domain": [1, 5] },
      "legend": { "title": "rank", "labelFontSize": 12 }
    }
  }
}
```

- [ ] **Step 2: Verify the JSON parses**

Run: `node -e "JSON.parse(require('fs').readFileSync('talks/vectordb-201/hybrid-fusion.json','utf8')); console.log('ok')"`

Expected: prints `ok`.

- [ ] **Step 3: Append the slide**

Append:

````markdown
---

# Same query, four columns

Pick a query. See where each retriever ranks the same five candidate docs.

```vega
- spec: ./hybrid-fusion.json
  renderer: svg
  actions: false
```

Notice the `INV-204871` query: BM25 nails the invoice, dense buries it. The fused column is what your user actually sees.
````

- [ ] **Step 4: Build, verify, test, commit**

Run: `npm run build talks/vectordb-201 && grep -c '<section' talks/vectordb-201/dist/index.html`

Expected: build succeeds, count is `22`.

Run: `npm test`

Expected: pass.

```bash
git add talks/vectordb-201/hybrid-fusion.json talks/vectordb-201/slides.md
git commit -m "feat(vectordb-201): add hybrid fusion four-column visualization"
```

---

## Task 9: Section 2 close — rerankers, two-stage architecture, Milvus, pitfalls

**Files:**
- Modify: `talks/vectordb-201/slides.md` (append 5 slides)

**Cumulative slide count after task: 27**

- [ ] **Step 1: Append the five closer slides**

Append:

````markdown
---

# Rerankers — cross > bi, slowly

Bi-encoders embed the query and the doc independently, then dot-product. That's what your dense retriever already does — fast, cheap, decent.

Cross-encoders feed `(query, doc)` *together* through the model and produce a relevance score. Much better signal — at the cost of one model call per candidate.

| | Bi-encoder | Cross-encoder |
|---|---|---|
| Latency | ~1ms / doc | ~10–20ms / doc |
| Quality | good | great |
| Use as | first-stage retriever | second-stage reranker |

You can't cross-encode a million docs per query. You can cross-encode the top 50 from a hybrid retrieval. That's the whole game.

---

# When reranking pays for itself

Concrete latency cost on a CPU reranker:

- ColBERT-style (lightweight): ~50ms for top-100
- Full cross-encoder (e.g., MiniLM): ~150–250ms for top-100
- LLM-as-reranker (e.g., GPT-4-class): ~500–2000ms

Worth it when:

- The top-1 result quality drives your business metric (search, support routing)
- You can budget the latency

Skip it when:

- Top-k > 20 and the LLM downstream will rerank anyway (RAG)
- p99 must be sub-100ms end-to-end

---

# Two-stage retrieval

```dot
rankdir=LR
Q [label="Query"]
H [label="Hybrid retriever\n(BM25 + dense + RRF)"]
T100 [label="Top-100"]
X [label="Cross-encoder\nreranker"]
T10 [label="Top-10"]
Q -> H -> T100 -> X -> T10
```

100 candidates is the sweet spot. Big enough to contain the right answer almost always; small enough that a 200ms reranker pass is tolerable.

---

# Milvus hybrid + reranker

```python
from pymilvus import MilvusClient, AnnSearchRequest, RRFRanker

dense_req  = AnnSearchRequest(data=[embed(q)], anns_field="vector",
                              param={"nprobe": 16}, limit=100)
sparse_req = AnnSearchRequest(data=[bm25_sparse(q)], anns_field="sparse",
                              param={}, limit=100)

results = client.hybrid_search(
    collection_name="docs",
    reqs=[dense_req, sparse_req],
    ranker=RRFRanker(k=60),
    limit=10,
)
```

Cross-encoder reranking happens after, in your application. Milvus returns the top-k fused; you call your reranker on the candidates and resort.

---

# Hybrid pitfalls

- **Score normalization is a tarpit** — if you must do weighted sum, use rank-percentiles, not raw scores. RRF avoids the problem entirely.
- **BM25 corpus drift** — IDF depends on the whole corpus. Streaming ingest changes it under your feet. Recompute periodically.
- **Reranking too aggressively** — running a cross-encoder on top-1000 is wasteful. Top-50 to top-100 is the band.
- **Forgetting to re-evaluate** — your hybrid weights and reranker model both go stale. Treat them as code.
````

- [ ] **Step 2: Build, verify dot count, test, commit**

Run: `npm run build talks/vectordb-201 && grep -c '<section' talks/vectordb-201/dist/index.html`

Expected: build succeeds, count is `27`.

Run: `grep -c 'class="dot"' talks/vectordb-201/dist/index.html`

Expected: `4`.

Run: `npm test`

Expected: pass.

```bash
git add talks/vectordb-201/slides.md
git commit -m "feat(vectordb-201): close Section 2 with rerankers and pitfalls"
```

---

## Task 10: Section 3 opener — distribution model through replica math

**Files:**
- Modify: `talks/vectordb-201/slides.md` (append section divider + 5 slides)

**Cumulative slide count after task: 33** (27 + 1 divider + 5 content)

- [ ] **Step 1: Append the section divider and five slides**

Append:

````markdown
---

{.section}
# Distribution & isolation
## 03

---

# The unit of scale

```text
collection
  └── shard       (horizontal partition, fixed at create time)
       └── segment   (immutable file, ~1 GB, the index lives here)
            └── index   (HNSW/IVF/DiskANN over the segment's vectors)
```

Each level has a job:

- **Collection** — schema and access boundary
- **Shard** — parallelism unit; queries fan out, results merge
- **Segment** — the unit of compaction, replication, and cache
- **Index** — the unit of recall/latency tuning

Tuning happens at every level. Most operators only think about the top one.

---

# The query path

```dot
rankdir=LR
C [label="Client"]
P [label="Proxy /\nCoordinator"]
S1 [label="Shard 1\nreplicas R1, R2"]
S2 [label="Shard 2\nreplicas R1, R2"]
S3 [label="Shard 3\nreplicas R1, R2"]
M [label="Merge\n+ top-k"]
R [label="Result"]
C -> P
P -> S1
P -> S2
P -> S3
S1 -> M
S2 -> M
S3 -> M
M -> R
```

Fan-out, fan-in. Latency is `max(per-shard) + merge`. One slow replica anywhere on the path stretches the whole query.

---

# Sharding strategies

| Strategy | Where vectors land | Wins for |
|---|---|---|
| **Hash on PK** | uniform random | balanced load, no skew, default |
| **Custom partition key** | grouped by `tenant_id` / `region` | data locality, tenant pinning, GDPR boundaries |

Hash sharding is the right default. Reach for partition keys when you have:

- A multi-tenant pattern where most queries scope to one tenant
- A regulatory boundary that must keep data physically separated
- A predictable hot tenant you want isolated from cold ones

Number of shards is fixed at collection creation. Pick conservatively — easier to add replicas than to reshard.

---

# Replication

A replica is a full copy of every segment on a different node. Reads round-robin across replicas. Writes hit all of them (eventually).

| Consistency | Read sees writes from… | Latency hit |
|---|---|---|
| **Strong** | committed before the read | high — wait for sync |
| **Bounded** | within a staleness window (e.g., 5s) | medium |
| **Session** | the same session's prior writes | low |
| **Eventually** | whenever they propagate | none |

`Bounded` is the production default for vector workloads — you almost never need strict read-your-writes for a search query, and the latency win is real.

---

# How many replicas?

Two constraints, both must hold:

```text
replicas ≥ ceil(target_QPS / per_replica_QPS)
replicas ≥ failure_domain_count + 1
```

Worked example. Target: 5000 QPS, p99 budget 50ms. A single HNSW shard does ~1500 QPS at that latency. Three AZs.

- QPS constraint: `ceil(5000/1500) = 4`
- Failure constraint: `3 + 1 = 4`
- Verdict: **4 replicas** per shard.

Now multiply by shards. This is how vector DB bills get scary.
````

- [ ] **Step 2: Build, verify dot count, test, commit**

Run: `npm run build talks/vectordb-201 && grep -c '<section' talks/vectordb-201/dist/index.html`

Expected: build succeeds, count is `33`.

Run: `grep -c 'class="dot"' talks/vectordb-201/dist/index.html`

Expected: `5`.

Run: `npm test`

Expected: pass.

```bash
git add talks/vectordb-201/slides.md
git commit -m "feat(vectordb-201): open Section 3 with topology and replication"
```

---

## Task 11: Section 3 close — multi-tenancy, noisy neighbours, Milvus config, decision flow

**Files:**
- Modify: `talks/vectordb-201/slides.md` (append 6 slides)

**Cumulative slide count after task: 39**

- [ ] **Step 1: Append the six slides**

Append:

````markdown
---

# Multi-tenancy — three patterns

| Pattern | Isolation | Blast radius | Ops cost | Tenant ceiling |
|---|---|---|---|---|
| **Collection per tenant** | strongest | one tenant | high (N collections) | ~thousands |
| **Partition key** | medium | shared shards, isolated by key | low | millions |
| **RBAC only** | weakest | shared everything | lowest | unlimited |

Pick the strongest isolation you can afford. Most production systems land on **partition key** — it gives data locality and predictable routing without the operational burden of N collections.

Collection-per-tenant is right when tenants have different schemas, very different data sizes, or hard regulatory boundaries.

---

# Noisy neighbour — three flavours

- **Query interference** — a tenant's query burst spikes p99 across all tenants on the shard
- **Ingest interference** — a tenant's bulk load saturates the write path
- **Index-build interference** — a compaction or rebuild eats CPU during peak query traffic

The metrics look the same: latency goes up, error rate creeps up, no obvious cause. The fix is different for each. Isolation primitives target different parts of this — partition keys for data, resource groups for compute.

---

# Resource groups

Milvus's primitive for pinning workloads to nodes. A resource group is a labelled subset of query nodes; collections (or partition keys) can be assigned to it.

Use cases:

- Pin a hot tenant to dedicated query nodes
- Separate production from canary traffic on the same cluster
- Reserve nodes for index builds so they don't steal query compute

Default group catches everything unassigned. Keep it sized for the long tail.

---

# Failure modes worth watching

- **Coordinator instability** — leader election storms; symptom is brief read unavailability across the whole cluster
- **Segment compaction stalls** — disk fills with un-compacted segments; symptom is slowly growing query latency
- **Index-build queue overflow** — new vectors arrive faster than the builder can absorb; symptom is recall slowly drifting downward as inserts stay un-indexed
- **Replica drift** — one replica's segment cache is cold; symptom is bimodal latency

The pattern: failure modes hide in metrics, not exceptions. Section 4 is about catching them.

---

# Milvus topology config

```python
from pymilvus import MilvusClient, DataType

schema = MilvusClient.create_schema(auto_id=True, partition_key_field="tenant_id")
schema.add_field("id",        DataType.INT64,        is_primary=True)
schema.add_field("tenant_id", DataType.VARCHAR, max_length=64)
schema.add_field("vector",    DataType.FLOAT_VECTOR, dim=768)
schema.add_field("text",      DataType.VARCHAR, max_length=8192)

client.create_collection(
    collection_name="docs",
    schema=schema,
    num_shards=4,
    num_partitions=64,    # partition-key buckets
)

client.load_collection("docs", replica_number=4, resource_groups=["prod"])
```

Four lines of topology config that took us six slides to motivate.

---

# Picking your topology

| If your data… | Pick this shape |
|---|---|
| < 10M vectors, single tenant | 1 shard, 2 replicas, hash routing |
| < 100M, single tenant, high QPS | 2–4 shards, 3+ replicas, hash routing |
| Multi-tenant, < 1000 tenants | partition-key on `tenant_id`, 2–4 shards, 3 replicas |
| Multi-tenant, hot tenant exists | partition-key + resource group for the hot tenant |
| > 1B vectors | shard count grows; consider DiskANN to control cost |

Reshard later is painful. Add replicas later is cheap. When in doubt, start fewer shards and more replicas.
````

- [ ] **Step 2: Build, verify, test, commit**

Run: `npm run build talks/vectordb-201 && grep -c '<section' talks/vectordb-201/dist/index.html`

Expected: build succeeds, count is `39`.

Run: `npm test`

Expected: pass.

```bash
git add talks/vectordb-201/slides.md
git commit -m "feat(vectordb-201): close Section 3 with multi-tenancy and topology decision"
```

---

## Task 12: Section 4 opener — golden signals through index build queue

**Files:**
- Modify: `talks/vectordb-201/slides.md` (append section divider + 5 slides)

**Cumulative slide count after task: 45** (39 + 1 divider + 5 content)

- [ ] **Step 1: Append the section divider and five slides**

Append:

````markdown
---

{.section}
# Observability & alerting
## 04

---

# Four golden signals, vector-DB edition

| Signal | What you measure |
|---|---|
| **Latency** | p50/p99/p99.9 of `search` and `insert`, by collection |
| **Errors** | non-2xx rate, timeout rate |
| **Traffic** | QPS in, vectors/sec ingested |
| **Saturation** | CPU, memory, GPU, segment cache hit rate, index-build queue depth |

These will catch most outages. They will not catch the worst one.

---

# The fifth signal — recall regression

Latency stays flat. Error rate stays flat. Saturation looks fine. Result *quality* quietly degrades.

Causes:

- A new embedding model deployed on one side of the index/query split
- Index params silently re-tuned
- Streaming ingest with stale IDF in the BM25 path
- A bug in a chunking pipeline, weeks ago

You will not notice this from the four golden signals. You will notice when the business metric drops six weeks later.

---

# Measuring recall in prod

Build a frozen ground-truth set: 1000–10,000 (query, expected-top-k) pairs that you trust.

- **Replay** the query set against the live index every hour / day
- **Compare** observed top-k against expected top-k → recall@10
- **Alert** on any regression beyond a threshold (e.g., -2% week-over-week)

Tradeoff: bigger query set → better signal, more replay cost. 1000 pairs replayed hourly is cheap and almost always enough.

The ground-truth set itself is what you're really protecting. Treat it like code: version it, review changes.

---

# p99 latency — what's normal

Per-index baselines, give or take:

| Index | Healthy p99 | Alert threshold (2× baseline) |
|---|---|---|
| HNSW (in-RAM) | 5–20 ms | 40 ms |
| IVF_PQ | 10–40 ms | 80 ms |
| DiskANN | 30–80 ms | 160 ms |

*These are end-to-end p99s — network, fan-out, and result merge included. The index-internal floors from slide 15 are lower.*

Set the alert threshold to 2× the rolling 7-day baseline, not an absolute number — your baseline drifts as the dataset grows. Page on sustained breach (5+ minutes), not a single spike.

---

# Ingest lag and build queue

The two leading indicators of "something's about to break":

- **Ingest lag** — vectors written → vectors searchable. Should be seconds. If it grows, the index builder is falling behind.
- **Build queue depth** — segments waiting to be indexed. A growing queue means recall is silently dropping (un-indexed vectors are flat-scanned, slowly).

Alert when either trends up over 10+ minutes. By the time latency moves, you're already late.
````

- [ ] **Step 2: Build, verify, test, commit**

Run: `npm run build talks/vectordb-201 && grep -c '<section' talks/vectordb-201/dist/index.html`

Expected: build succeeds, count is `45`.

Run: `npm test`

Expected: pass.

```bash
git add talks/vectordb-201/slides.md
git commit -m "feat(vectordb-201): open Section 4 with golden signals and recall regression"
```

---

## Task 13: Section 4 close — memory, tail, alerts, dashboard, three alerts

**Files:**
- Modify: `talks/vectordb-201/slides.md` (append 5 slides)

**Cumulative slide count after task: 50**

- [ ] **Step 1: Append the five slides**

Append:

````markdown
---

# Memory pressure and cache evictions

For DiskANN and IVF_PQ-on-disk, the segment cache is doing the work. Watch:

- **Cache hit rate** — should be 95%+. Drops mean queries are hitting cold disk.
- **Eviction rate** — frequent evictions mean working set > cache size; you're paying SSD latency on the path.
- **Page-fault rate** — for memory-mapped indexes, a leading indicator of cache thrash.

For HNSW, watch RSS vs. configured limit. HNSW has no cache to thrash — it OOMs.

---

# p99.9 — where the lies live

p99 looks fine. Most users are fine. But your top 0.1% of queries — the slow ones — are exactly the ones that break SLAs and the ones that hurt the noisiest tenant most.

p99.9 catches:

- Cold-cache queries on DiskANN
- Queries that fan out to a slow replica
- Index-build interference during peak hours
- Tenant-skew in a partition-key setup

Multi-tenancy fairness lives at p99.9, not p99. If you only watch p99, you will believe your shared cluster is fine until a customer escalates.

---

# Alert pyramid

| Tier | Latency to ack | Examples |
|---|---|---|
| **Page** | 5 min | error rate > 1%, p99 sustained 2× baseline, recall regression > 5% |
| **Ticket** | 1 day | ingest lag growing, cache hit rate dropping, p99.9 drifting |
| **Log/dashboard** | review weekly | per-tenant fairness, build-queue depth, segment count |

If everything pages, nothing pages. Demote ruthlessly — most signals belong on the dashboard, not in your phone.

---

# A monitoring layout that works

```dot
rankdir=TB
node [shape=box, style="rounded,filled", fontsize=11]

subgraph cluster_top {
  label="Top of dashboard"
  color="#1f6feb"
  style=dashed
  L [label="p99 latency\n(per collection, last 24h)"]
  E [label="error rate\n(per endpoint, last 24h)"]
  R [label="recall@10\n(replay job, last 7d)"]
}

subgraph cluster_mid {
  label="Middle"
  color="#9aa4b2"
  style=dashed
  Q [label="QPS in\nvs. capacity"]
  I [label="ingest lag\n+ build queue depth"]
  C [label="cache hit rate\n+ evictions"]
}

subgraph cluster_bottom {
  label="Bottom (drill-down)"
  color="#9aa4b2"
  style=dashed
  T [label="per-tenant\np99.9"]
  S [label="per-shard\nlatency"]
  N [label="node-level\nCPU / mem / GPU"]
}

L -> Q [style=invis]
Q -> T [style=invis]
```

Top row pages. Middle row tickets. Bottom row tells you where to look when something does page.

---

# If you only set up three alerts

1. **`recall@10` regression** — the silent killer
2. **`p99 search latency`** sustained > 2× rolling baseline
3. **`ingest lag`** trending up over 10+ minutes

Everything else is debugging. These three catch the production failures that actually matter.
````

- [ ] **Step 2: Build, verify dot count (now 6 with dashboard), test, commit**

Run: `npm run build talks/vectordb-201 && grep -c '<section' talks/vectordb-201/dist/index.html`

Expected: build succeeds, count is `50`.

Run: `grep -c 'class="dot"' talks/vectordb-201/dist/index.html`

Expected: `6`.

Run: `npm test`

Expected: pass.

```bash
git add talks/vectordb-201/slides.md
git commit -m "feat(vectordb-201): close Section 4 with alert pyramid and dashboard"
```

---

## Task 14: Outro slides + bundle test + visual polish pass

**Files:**
- Modify: `talks/vectordb-201/slides.md` (append 3 slides)

**Cumulative slide count after task: 53**

- [ ] **Step 1: Append the three outro slides**

Append:

````markdown
---

# What's next

If this clicked, the **301 talk** covers cost engineering end-to-end:

- Quantization — PQ, SQ, binary, when each pays off
- Tiered storage — hot in RAM, warm on SSD, cold in object store
- GPU economics — when GPU indexes beat CPU, when they don't
- The math of "vector DB is 40% of my AI bill"

Same shape, same voice, different axis: $/recall instead of latency/recall.

---

{.center}
# Resources

- **Milvus docs** — milvus.io/docs
- **Zilliz Learn** — zilliz.com/learn
- **DiskANN paper** — Subramanya et al., NeurIPS 2019
- **HNSW paper** — Malkov & Yashunin, 2018
- **This deck** — github.com/simonhearne/presentations

---

{.hero .no-chrome}
# Production isn't an index. It's a system.
````

- [ ] **Step 2: Build the deck**

Run: `npm run build talks/vectordb-201`

Expected: exits 0.

- [ ] **Step 3: Verify final slide count**

Run: `grep -c '<section' talks/vectordb-201/dist/index.html`

Expected: `53`.

- [ ] **Step 4: Verify final dot count**

Run: `grep -c 'class="dot"' talks/vectordb-201/dist/index.html`

Expected: `6`.

- [ ] **Step 5: Verify all three Vega specs are referenced and inlined as base64 data URIs**

Run: `grep -o 'data:application/json;base64' talks/vectordb-201/dist/index.html | wc -l`

Expected: `3` (one per Vega chart).

- [ ] **Step 6: Test the bundle pipeline**

Run: `npm run bundle talks/vectordb-201`

Expected: exits 0, produces `talks/vectordb-201/dist/bundle.html`. The bundle should be a self-contained file that opens directly in a browser.

- [ ] **Step 7: Verify the bundle is a single self-contained file**

Run: `ls -lh talks/vectordb-201/dist/bundle.html`

Expected: file exists, typically 1–4 MB.

Run: `grep -c 'src="\\./' talks/vectordb-201/dist/bundle.html`

Expected: `0` (no remaining relative `./` asset references — everything inlined).

- [ ] **Step 8: Run all tests**

Run: `npm test`

Expected: all tests pass.

- [ ] **Step 9: Manual visual review checklist**

Open `talks/vectordb-201/dist/index.html` in a browser. Walk the deck and confirm:

- [ ] Title slide renders with logo, author card, and brand chrome
- [ ] All four `{.section}` dividers display the section number
- [ ] Tables render with brand styling (no overflow, readable on 1920×1080)
- [ ] All six dot diagrams render (no zero-size figures, text not clipped)
- [ ] All three Vega charts render and respond to inputs:
  - IVF Voronoi: clicking moves the query diamond, slider changes probed cells
  - Recall vs latency: dropdown switches dataset size, all three curves visible
  - Hybrid fusion: dropdown switches query, color encoding updates
- [ ] Code blocks have brand syntax highlighting
- [ ] Hero closer slide displays at full bleed

If any item fails, file is in `talks/vectordb-201/slides.md` — fix in place.

- [ ] **Step 10: Commit**

```bash
git add talks/vectordb-201/slides.md
git commit -m "feat(vectordb-201): add outro and ship the deck"
```

- [ ] **Step 11: Final verification — full slide count summary**

Run: `grep -c '<section' talks/vectordb-201/dist/index.html`

Expected: `53` — matching: 3 intro + 4 section dividers + (12 + 10 + 11 + 10) section slides + 3 outro = 53.

---

## Done

The deck is shipped to `main`. To view:

- Built form: `talks/vectordb-201/dist/index.html` (relies on adjacent assets)
- Bundle: `talks/vectordb-201/dist/bundle.html` (single file, share-friendly)
